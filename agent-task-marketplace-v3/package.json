{
  "name": "agent-task-marketplace",
  "version": "3.0.0",
  "description": "OpenClaw skill for Agent Task Marketplace — receive job broadcasts via Socket.IO, notify user with match score, await approval, run executor, then submit bid.",
  "main": "scripts/listen.js",
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "engines": {
    "node": ">=18"
  }
}
